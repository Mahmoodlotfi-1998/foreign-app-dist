name: spa-app
services:
  web:
    build_type: dockerfile
    dockerfile_path: ./Dockerfile  # Path to the Dockerfile
    context_path: .               # Context is the current directory
    ports:
      - "7070:80"                 # Map port 8080 on the host to port 80 in the container
